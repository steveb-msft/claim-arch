# Proposed GitHub Issues — CLM Platform

## Summary
| Repository | Issue Count |
|---|---:|
| claim-clm-infra | 73 |
| claim-clm-storage | 26 |
| claim-clm | 81 |
| claim-clm-ai | 63 |
| claim-clm-integration | 29 |
| claim-clm-portal | 14 |
| **Total** | **286** |

## Phase 0 — Infrastructure Foundation

### claim-clm-infra
| # | Issue Title | Labels | Description |
|---:|---|---|---|
| 1 | CLM-013: Design AKS Cluster architecture | `infrastructure` | Define the Phase 0 reference architecture for AKS Cluster in claim-clm-infra/modules/aks, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 2 | CLM-013: Implement Terraform or Bicep for AKS Cluster | `infrastructure` | Author reusable Terraform or Bicep modules, Helm values, and Azure policy assets for AKS Cluster within modules/aks. Ensure naming, private networking, secret references, RBAC bindings, and outputs are standardized so downstream environments can consume the platform layer consistently. |
| 3 | CLM-013: Configure policy and pipeline support for AKS Cluster | `infrastructure` | Add the delivery, policy, and operational plumbing for AKS Cluster so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 0 readiness. |
| 4 | CLM-013: Document operations for AKS Cluster | `infrastructure`, `documentation` | Document how AKS Cluster is provisioned, validated, and operated in claim-clm-infra, including environment prerequisites, emergency procedures, and rollback steps. The final runbook should be detailed enough for platform engineers and on-call responders to support the feature without tribal knowledge. |
| 5 | CLM-053: Design Network Isolation and Private Endpoints architecture | `infrastructure` | Define the Phase 0 reference architecture for Network Isolation and Private Endpoints in claim-clm-infra/modules/networking, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 6 | CLM-053: Implement Terraform or Bicep for Network Isolation and Private Endpoints | `infrastructure` | Author reusable Terraform or Bicep modules, Helm values, and Azure policy assets for Network Isolation and Private Endpoints within modules/networking. Ensure naming, private networking, secret references, RBAC bindings, and outputs are standardized so downstream environments can consume the platform layer consistently. |
| 7 | CLM-053: Configure policy and pipeline support for Network Isolation and Private Endpoints | `infrastructure` | Add the delivery, policy, and operational plumbing for Network Isolation and Private Endpoints so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 0 readiness. |
| 8 | CLM-053: Document operations for Network Isolation and Private Endpoints | `infrastructure`, `documentation` | Document how Network Isolation and Private Endpoints is provisioned, validated, and operated in claim-clm-infra, including environment prerequisites, emergency procedures, and rollback steps. The final runbook should be detailed enough for platform engineers and on-call responders to support the feature without tribal knowledge. |
| 9 | CLM-016: Design Secrets Management architecture | `infrastructure`, `security` | Define the Phase 0 reference architecture for Secrets Management in claim-clm-infra/modules/security, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 10 | CLM-016: Implement Terraform or Bicep for Secrets Management | `infrastructure`, `security` | Author reusable Terraform or Bicep modules, Helm values, and Azure policy assets for Secrets Management within modules/security. Ensure naming, private networking, secret references, RBAC bindings, and outputs are standardized so downstream environments can consume the platform layer consistently. |
| 11 | CLM-016: Configure policy and pipeline support for Secrets Management | `infrastructure`, `security` | Add the delivery, policy, and operational plumbing for Secrets Management so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 0 readiness. |
| 12 | CLM-016: Document operations for Secrets Management | `infrastructure`, `security`, `documentation` | Document how Secrets Management is provisioned, validated, and operated in claim-clm-infra, including environment prerequisites, emergency procedures, and rollback steps. The final runbook should be detailed enough for platform engineers and on-call responders to support the feature without tribal knowledge. |
| 13 | CLM-012: Design Encryption at Rest and in Transit architecture | `infrastructure`, `security` | Define the Phase 0 reference architecture for Encryption at Rest and in Transit in claim-clm-infra/modules/security, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 14 | CLM-012: Implement Terraform or Bicep for Encryption at Rest and in Transit | `infrastructure`, `security` | Author reusable Terraform or Bicep modules, Helm values, and Azure policy assets for Encryption at Rest and in Transit within modules/security. Ensure naming, private networking, secret references, RBAC bindings, and outputs are standardized so downstream environments can consume the platform layer consistently. |
| 15 | CLM-012: Configure policy and pipeline support for Encryption at Rest and in Transit | `infrastructure`, `security` | Add the delivery, policy, and operational plumbing for Encryption at Rest and in Transit so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 0 readiness. |
| 16 | CLM-012: Document operations for Encryption at Rest and in Transit | `infrastructure`, `security`, `documentation` | Document how Encryption at Rest and in Transit is provisioned, validated, and operated in claim-clm-infra, including environment prerequisites, emergency procedures, and rollback steps. The final runbook should be detailed enough for platform engineers and on-call responders to support the feature without tribal knowledge. |
| 17 | CLM-014: Design Ingress and API Gateway architecture | `infrastructure`, `integration` | Define the Phase 0 reference architecture for Ingress and API Gateway in claim-clm-infra/modules/gateway, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 18 | CLM-014: Implement Terraform or Bicep for Ingress and API Gateway | `infrastructure`, `integration` | Author reusable Terraform or Bicep modules, Helm values, and Azure policy assets for Ingress and API Gateway within modules/gateway. Ensure naming, private networking, secret references, RBAC bindings, and outputs are standardized so downstream environments can consume the platform layer consistently. |
| 19 | CLM-014: Configure policy and pipeline support for Ingress and API Gateway | `infrastructure`, `integration` | Add the delivery, policy, and operational plumbing for Ingress and API Gateway so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 0 readiness. |
| 20 | CLM-014: Document operations for Ingress and API Gateway | `infrastructure`, `integration`, `documentation` | Document how Ingress and API Gateway is provisioned, validated, and operated in claim-clm-infra, including environment prerequisites, emergency procedures, and rollback steps. The final runbook should be detailed enough for platform engineers and on-call responders to support the feature without tribal knowledge. |
| 21 | CLM-010: Design Authentication and SSO reference architecture | `infrastructure`, `security` | Define the Phase 0 reference architecture for Authentication and SSO in claim-clm-infra/modules/security, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 22 | CLM-010: Provision Terraform and Helm baseline for Authentication and SSO | `infrastructure`, `security` | Author reusable Terraform or Bicep modules, Helm values, and Azure policy assets for Authentication and SSO within modules/security. Ensure naming, private networking, secret references, RBAC bindings, and outputs are standardized so downstream environments can consume the platform layer consistently. |
| 23 | CLM-010: Configure policy, GitOps, and release controls for Authentication and SSO | `infrastructure`, `security` | Add the delivery, policy, and operational plumbing for Authentication and SSO so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 0 readiness. |
| 24 | CLM-010: Validate resilience and security for Authentication and SSO | `infrastructure`, `security`, `testing` | Add validation coverage for Authentication and SSO using policy checks, environment smoke tests, and failure-path exercises. The final test suite should prove the infrastructure can be promoted safely and that operations teams can detect drift or broken dependencies quickly. |
| 25 | CLM-010: Publish runbooks for Authentication and SSO | `infrastructure`, `security`, `documentation` | Document how Authentication and SSO is provisioned, validated, and operated in claim-clm-infra, including environment prerequisites, emergency procedures, and rollback steps. The final runbook should be detailed enough for platform engineers and on-call responders to support the feature without tribal knowledge. |
| 26 | CLM-018: Design Entra ID Integration architecture | `infrastructure`, `security` | Define the Phase 0 reference architecture for Entra ID Integration in claim-clm-infra/modules/security, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 27 | CLM-018: Implement Terraform or Bicep for Entra ID Integration | `infrastructure`, `security` | Author reusable Terraform or Bicep modules, Helm values, and Azure policy assets for Entra ID Integration within modules/security. Ensure naming, private networking, secret references, RBAC bindings, and outputs are standardized so downstream environments can consume the platform layer consistently. |
| 28 | CLM-018: Configure policy and pipeline support for Entra ID Integration | `infrastructure`, `security` | Add the delivery, policy, and operational plumbing for Entra ID Integration so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 0 readiness. |
| 29 | CLM-018: Document operations for Entra ID Integration | `infrastructure`, `security`, `documentation` | Document how Entra ID Integration is provisioned, validated, and operated in claim-clm-infra, including environment prerequisites, emergency procedures, and rollback steps. The final runbook should be detailed enough for platform engineers and on-call responders to support the feature without tribal knowledge. |
| 30 | CLM-011: Design Role-Based Access Control architecture | `infrastructure` | Define the Phase 0 reference architecture for Role-Based Access Control in claim-clm-infra/modules/security, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 31 | CLM-011: Implement Terraform or Bicep for Role-Based Access Control | `infrastructure` | Author reusable Terraform or Bicep modules, Helm values, and Azure policy assets for Role-Based Access Control within modules/security. Ensure naming, private networking, secret references, RBAC bindings, and outputs are standardized so downstream environments can consume the platform layer consistently. |
| 32 | CLM-011: Configure policy and pipeline support for Role-Based Access Control | `infrastructure` | Add the delivery, policy, and operational plumbing for Role-Based Access Control so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 0 readiness. |
| 33 | CLM-011: Document operations for Role-Based Access Control | `infrastructure`, `documentation` | Document how Role-Based Access Control is provisioned, validated, and operated in claim-clm-infra, including environment prerequisites, emergency procedures, and rollback steps. The final runbook should be detailed enough for platform engineers and on-call responders to support the feature without tribal knowledge. |
| 34 | CLM-015: Design GitOps and CI/CD reference architecture | `infrastructure` | Define the Phase 0 reference architecture for GitOps and CI/CD in claim-clm-infra/gitops/, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 35 | CLM-015: Provision Terraform and Helm baseline for GitOps and CI/CD | `infrastructure` | Author reusable Terraform or Bicep modules, Helm values, and Azure policy assets for GitOps and CI/CD within gitops/. Ensure naming, private networking, secret references, RBAC bindings, and outputs are standardized so downstream environments can consume the platform layer consistently. |
| 36 | CLM-015: Configure policy, GitOps, and release controls for GitOps and CI/CD | `infrastructure` | Add the delivery, policy, and operational plumbing for GitOps and CI/CD so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 0 readiness. |
| 37 | CLM-015: Validate resilience and security for GitOps and CI/CD | `infrastructure`, `testing` | Add validation coverage for GitOps and CI/CD using policy checks, environment smoke tests, and failure-path exercises. The final test suite should prove the infrastructure can be promoted safely and that operations teams can detect drift or broken dependencies quickly. |
| 38 | CLM-015: Publish runbooks for GitOps and CI/CD | `infrastructure`, `documentation` | Document how GitOps and CI/CD is provisioned, validated, and operated in claim-clm-infra, including environment prerequisites, emergency procedures, and rollback steps. The final runbook should be detailed enough for platform engineers and on-call responders to support the feature without tribal knowledge. |
| 39 | CLM-017: Design Observability and Monitoring reference architecture | `infrastructure` | Define the Phase 0 reference architecture for Observability and Monitoring in claim-clm-infra/modules/monitoring, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 40 | CLM-017: Provision Terraform and Helm baseline for Observability and Monitoring | `infrastructure` | Author reusable Terraform or Bicep modules, Helm values, and Azure policy assets for Observability and Monitoring within modules/monitoring. Ensure naming, private networking, secret references, RBAC bindings, and outputs are standardized so downstream environments can consume the platform layer consistently. |
| 41 | CLM-017: Configure policy, GitOps, and release controls for Observability and Monitoring | `infrastructure` | Add the delivery, policy, and operational plumbing for Observability and Monitoring so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 0 readiness. |
| 42 | CLM-017: Validate resilience and security for Observability and Monitoring | `infrastructure`, `testing` | Add validation coverage for Observability and Monitoring using policy checks, environment smoke tests, and failure-path exercises. The final test suite should prove the infrastructure can be promoted safely and that operations teams can detect drift or broken dependencies quickly. |
| 43 | CLM-017: Publish runbooks for Observability and Monitoring | `infrastructure`, `documentation` | Document how Observability and Monitoring is provisioned, validated, and operated in claim-clm-infra, including environment prerequisites, emergency procedures, and rollback steps. The final runbook should be detailed enough for platform engineers and on-call responders to support the feature without tribal knowledge. |

## Phase 1 — Core Platform

### claim-clm-storage
| # | Issue Title | Labels | Description |
|---:|---|---|---|
| 1 | CLM-003: Design Blob Document Storage API and data model | `enhancement` | Define the Phase 1 reference architecture for Blob Document Storage in claim-clm-storage/clm-storage-api, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 2 | CLM-003: Implement Blob Document Storage service workflow | `enhancement` | Implement storage APIs, persistence changes, and background processing for Blob Document Storage in claim-clm-storage/clm-storage-api. Cover lifecycle rules, metadata consistency, retry behavior, and failure recovery between blob storage and any dependent indexing or retrieval components. |
| 3 | CLM-003: Add tests and docs for Blob Document Storage | `enhancement`, `testing`, `documentation` | Create unit, integration, and regression coverage for Blob Document Storage with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 4 | CLM-004: Design Document Metadata Store API and data model | `enhancement` | Define the Phase 1 reference architecture for Document Metadata Store in claim-clm-storage/clm-storage-api, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 5 | CLM-004: Implement Document Metadata Store service workflow | `enhancement` | Implement storage APIs, persistence changes, and background processing for Document Metadata Store in claim-clm-storage/clm-storage-api. Cover lifecycle rules, metadata consistency, retry behavior, and failure recovery between blob storage and any dependent indexing or retrieval components. |
| 6 | CLM-004: Add tests and docs for Document Metadata Store | `enhancement`, `testing`, `documentation` | Create unit, integration, and regression coverage for Document Metadata Store with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 7 | CLM-005: Design Document Retrieval API API and data model | `enhancement`, `integration` | Define the Phase 1 reference architecture for Document Retrieval API in claim-clm-storage/clm-storage-api, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 8 | CLM-005: Implement Document Retrieval API service workflow | `enhancement`, `integration` | Implement storage APIs, persistence changes, and background processing for Document Retrieval API in claim-clm-storage/clm-storage-api. Cover lifecycle rules, metadata consistency, retry behavior, and failure recovery between blob storage and any dependent indexing or retrieval components. |
| 9 | CLM-005: Add tests and docs for Document Retrieval API | `enhancement`, `integration`, `testing`, `documentation` | Create unit, integration, and regression coverage for Document Retrieval API with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 10 | CLM-006: Design Document Indexing Pipeline API and data model | `enhancement`, `integration` | Define the Phase 1 reference architecture for Document Indexing Pipeline in claim-clm-storage/clm-doc-indexer, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 11 | CLM-006: Implement Document Indexing Pipeline service workflow | `enhancement`, `integration` | Implement storage APIs, persistence changes, and background processing for Document Indexing Pipeline in claim-clm-storage/clm-doc-indexer. Cover lifecycle rules, metadata consistency, retry behavior, and failure recovery between blob storage and any dependent indexing or retrieval components. |
| 12 | CLM-006: Add tests and docs for Document Indexing Pipeline | `enhancement`, `integration`, `testing`, `documentation` | Create unit, integration, and regression coverage for Document Indexing Pipeline with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |

### claim-clm
| # | Issue Title | Labels | Description |
|---:|---|---|---|
| 1 | CLM-002: Design Clause Library domain model | `enhancement` | Define the Phase 1 reference architecture for Clause Library in claim-clm/clm-api, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 2 | CLM-002: Implement Clause Library feature flow | `enhancement` | Build the domain logic, APIs, worker orchestration, and persistence changes required for Clause Library in claim-clm/clm-api. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 3 | CLM-002: Add tests and docs for Clause Library | `enhancement`, `testing`, `documentation` | Create unit, integration, and regression coverage for Clause Library with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 4 | CLM-001: Design Contract Template Engine domain model | `enhancement` | Define the Phase 1 reference architecture for Contract Template Engine in claim-clm/clm-template-service, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 5 | CLM-001: Implement Contract Template Engine feature flow | `enhancement` | Build the domain logic, APIs, worker orchestration, and persistence changes required for Contract Template Engine in claim-clm/clm-template-service. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 6 | CLM-001: Add tests and docs for Contract Template Engine | `enhancement`, `testing`, `documentation` | Create unit, integration, and regression coverage for Contract Template Engine with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 7 | CLM-035: Design REST API (Comprehensive) domain model | `enhancement`, `integration` | Define the Phase 1 reference architecture for REST API (Comprehensive) in claim-clm/clm-api, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 8 | CLM-035: Implement REST API (Comprehensive) feature flow | `enhancement`, `integration` | Build the domain logic, APIs, worker orchestration, and persistence changes required for REST API (Comprehensive) in claim-clm/clm-api. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 9 | CLM-035: Add tests and docs for REST API (Comprehensive) | `enhancement`, `integration`, `testing`, `documentation` | Create unit, integration, and regression coverage for REST API (Comprehensive) with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 10 | CLM-019: Design Immutable Audit Log domain model | `enhancement` | Define the Phase 1 reference architecture for Immutable Audit Log in claim-clm/clm-audit-writer, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 11 | CLM-019: Implement Immutable Audit Log feature flow | `enhancement` | Build the domain logic, APIs, worker orchestration, and persistence changes required for Immutable Audit Log in claim-clm/clm-audit-writer. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 12 | CLM-019: Add tests and docs for Immutable Audit Log | `enhancement`, `testing`, `documentation` | Create unit, integration, and regression coverage for Immutable Audit Log with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 13 | CLM-007: Design domain and API contract for Multi-Stage Approval Workflow | `enhancement` | Define the Phase 1 reference architecture for Multi-Stage Approval Workflow in claim-clm/clm-workflow-engine, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 14 | CLM-007: Implement core service logic for Multi-Stage Approval Workflow | `enhancement` | Build the domain logic, APIs, worker orchestration, and persistence changes required for Multi-Stage Approval Workflow in claim-clm/clm-workflow-engine. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 15 | CLM-007: Implement supporting UX or orchestration for Multi-Stage Approval Workflow | `enhancement` | Implement the supporting integration, UI, or orchestration work needed to complete Multi-Stage Approval Workflow, especially across adjacent services and shared platform capabilities. Capture failure handling, idempotency, and backward compatibility so the feature behaves predictably under real contract workloads. |
| 16 | CLM-007: Add tests and docs for Multi-Stage Approval Workflow | `enhancement`, `testing`, `documentation` | Create unit, integration, and regression coverage for Multi-Stage Approval Workflow with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 17 | CLM-008: Design SLA Enforcement and Escalation domain model | `enhancement` | Define the Phase 1 reference architecture for SLA Enforcement and Escalation in claim-clm/clm-sla-enforcer, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 18 | CLM-008: Implement SLA Enforcement and Escalation feature flow | `enhancement` | Build the domain logic, APIs, worker orchestration, and persistence changes required for SLA Enforcement and Escalation in claim-clm/clm-sla-enforcer. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 19 | CLM-008: Add tests and docs for SLA Enforcement and Escalation | `enhancement`, `testing`, `documentation` | Create unit, integration, and regression coverage for SLA Enforcement and Escalation with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 20 | CLM-009: Design domain and API contract for Full-Text and Semantic Search | `enhancement`, `integration` | Define the Phase 1 reference architecture for Full-Text and Semantic Search in claim-clm/clm-search-service, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 21 | CLM-009: Implement core service logic for Full-Text and Semantic Search | `enhancement`, `integration` | Build the domain logic, APIs, worker orchestration, and persistence changes required for Full-Text and Semantic Search in claim-clm/clm-search-service. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 22 | CLM-009: Implement supporting UX or orchestration for Full-Text and Semantic Search | `enhancement`, `integration` | Implement the supporting integration, UI, or orchestration work needed to complete Full-Text and Semantic Search, especially across adjacent services and shared platform capabilities. Capture failure handling, idempotency, and backward compatibility so the feature behaves predictably under real contract workloads. |
| 23 | CLM-009: Add tests and docs for Full-Text and Semantic Search | `enhancement`, `integration`, `testing`, `documentation` | Create unit, integration, and regression coverage for Full-Text and Semantic Search with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |

## Phase 2 — Enhanced Capabilities

### claim-clm-infra
| # | Issue Title | Labels | Description |
|---:|---|---|---|
| 1 | CLM-039: Design Multi-Factor Authentication architecture | `infrastructure`, `security` | Define the Phase 2 reference architecture for Multi-Factor Authentication in claim-clm-infra/Entra Conditional Access, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 2 | CLM-039: Implement Terraform or Bicep for Multi-Factor Authentication | `infrastructure`, `security` | Author reusable Terraform or Bicep modules, Helm values, and Azure policy assets for Multi-Factor Authentication within Entra Conditional Access. Ensure naming, private networking, secret references, RBAC bindings, and outputs are standardized so downstream environments can consume the platform layer consistently. |
| 3 | CLM-039: Configure policy and pipeline support for Multi-Factor Authentication | `infrastructure`, `security` | Add the delivery, policy, and operational plumbing for Multi-Factor Authentication so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 2 readiness. |
| 4 | CLM-039: Document operations for Multi-Factor Authentication | `infrastructure`, `security`, `documentation` | Document how Multi-Factor Authentication is provisioned, validated, and operated in claim-clm-infra, including environment prerequisites, emergency procedures, and rollback steps. The final runbook should be detailed enough for platform engineers and on-call responders to support the feature without tribal knowledge. |

### claim-clm-storage
| # | Issue Title | Labels | Description |
|---:|---|---|---|
| 1 | CLM-023: Design Content Extraction and OCR API and data model | `enhancement` | Define the Phase 2 reference architecture for Content Extraction and OCR in claim-clm-storage/clm-doc-extractor, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 2 | CLM-023: Implement Content Extraction and OCR service workflow | `enhancement` | Implement storage APIs, persistence changes, and background processing for Content Extraction and OCR in claim-clm-storage/clm-doc-extractor. Cover lifecycle rules, metadata consistency, retry behavior, and failure recovery between blob storage and any dependent indexing or retrieval components. |
| 3 | CLM-023: Add tests and docs for Content Extraction and OCR | `enhancement`, `testing`, `documentation` | Create unit, integration, and regression coverage for Content Extraction and OCR with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 4 | CLM-024: Design Document Preview Service data flow and contracts | `enhancement` | Define the Phase 2 reference architecture for Document Preview Service in claim-clm-storage/clm-doc-preview, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 5 | CLM-024: Implement service components for Document Preview Service | `enhancement` | Implement storage APIs, persistence changes, and background processing for Document Preview Service in claim-clm-storage/clm-doc-preview. Cover lifecycle rules, metadata consistency, retry behavior, and failure recovery between blob storage and any dependent indexing or retrieval components. |
| 6 | CLM-024: Integrate storage and indexing dependencies for Document Preview Service | `enhancement`, `infrastructure` | Add the delivery, policy, and operational plumbing for Document Preview Service so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 2 readiness. |
| 7 | CLM-024: Add tests and operations guide for Document Preview Service | `enhancement`, `testing`, `documentation` | Create unit, integration, and regression coverage for Document Preview Service with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |

### claim-clm
| # | Issue Title | Labels | Description |
|---:|---|---|---|
| 1 | CLM-033: Design domain and API contract for Email and SMS Notifications | `enhancement` | Define the Phase 2 reference architecture for Email and SMS Notifications in claim-clm/clm-notification-worker, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 2 | CLM-033: Implement core service logic for Email and SMS Notifications | `enhancement` | Build the domain logic, APIs, worker orchestration, and persistence changes required for Email and SMS Notifications in claim-clm/clm-notification-worker. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 3 | CLM-033: Implement supporting UX or orchestration for Email and SMS Notifications | `enhancement` | Implement the supporting integration, UI, or orchestration work needed to complete Email and SMS Notifications, especially across adjacent services and shared platform capabilities. Capture failure handling, idempotency, and backward compatibility so the feature behaves predictably under real contract workloads. |
| 4 | CLM-033: Add tests and docs for Email and SMS Notifications | `enhancement`, `testing`, `documentation` | Create unit, integration, and regression coverage for Email and SMS Notifications with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 5 | CLM-034: Design domain and API contract for In-App Real-Time Notifications | `enhancement` | Define the Phase 2 reference architecture for In-App Real-Time Notifications in claim-clm/clm-realtime-hub, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 6 | CLM-034: Implement core service logic for In-App Real-Time Notifications | `enhancement` | Build the domain logic, APIs, worker orchestration, and persistence changes required for In-App Real-Time Notifications in claim-clm/clm-realtime-hub. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 7 | CLM-034: Implement supporting UX or orchestration for In-App Real-Time Notifications | `enhancement` | Implement the supporting integration, UI, or orchestration work needed to complete In-App Real-Time Notifications, especially across adjacent services and shared platform capabilities. Capture failure handling, idempotency, and backward compatibility so the feature behaves predictably under real contract workloads. |
| 8 | CLM-034: Add tests and docs for In-App Real-Time Notifications | `enhancement`, `testing`, `documentation` | Create unit, integration, and regression coverage for In-App Real-Time Notifications with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 9 | CLM-021: Design end-to-end architecture for Collaborative Real-Time Editing | `enhancement` | Define the Phase 2 reference architecture for Collaborative Real-Time Editing in claim-clm/clm-realtime-hub + clm-web, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 10 | CLM-021: Implement backend services for Collaborative Real-Time Editing | `enhancement` | Build the domain logic, APIs, worker orchestration, and persistence changes required for Collaborative Real-Time Editing in claim-clm/clm-realtime-hub + clm-web. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 11 | CLM-021: Implement UI, workflow, or integration support for Collaborative Real-Time Editing | `enhancement` | Implement the supporting integration, UI, or orchestration work needed to complete Collaborative Real-Time Editing, especially across adjacent services and shared platform capabilities. Capture failure handling, idempotency, and backward compatibility so the feature behaves predictably under real contract workloads. |
| 12 | CLM-021: Add tests and telemetry for Collaborative Real-Time Editing | `enhancement`, `testing` | Create unit, integration, and regression coverage for Collaborative Real-Time Editing with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 13 | CLM-021: Document APIs and rollout for Collaborative Real-Time Editing | `enhancement`, `documentation` | Document how Collaborative Real-Time Editing is configured, consumed, and supported in claim-clm, including API contracts, operational runbooks, and rollback guidance as applicable. The final documentation should be detailed enough for developers, QA, and platform support to onboard without tribal knowledge. |
| 14 | CLM-022: Design domain and API contract for Dynamic Form Builder | `enhancement` | Define the Phase 2 reference architecture for Dynamic Form Builder in claim-clm/clm-web + clm-api, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 15 | CLM-022: Implement core service logic for Dynamic Form Builder | `enhancement` | Build the domain logic, APIs, worker orchestration, and persistence changes required for Dynamic Form Builder in claim-clm/clm-web + clm-api. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 16 | CLM-022: Implement supporting UX or orchestration for Dynamic Form Builder | `enhancement` | Implement the supporting integration, UI, or orchestration work needed to complete Dynamic Form Builder, especially across adjacent services and shared platform capabilities. Capture failure handling, idempotency, and backward compatibility so the feature behaves predictably under real contract workloads. |
| 17 | CLM-022: Add tests and docs for Dynamic Form Builder | `enhancement`, `testing`, `documentation` | Create unit, integration, and regression coverage for Dynamic Form Builder with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 18 | CLM-027: Design domain and API contract for Redlining and Track Changes | `enhancement` | Define the Phase 2 reference architecture for Redlining and Track Changes in claim-clm/clm-api + clm-web, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 19 | CLM-027: Implement core service logic for Redlining and Track Changes | `enhancement` | Build the domain logic, APIs, worker orchestration, and persistence changes required for Redlining and Track Changes in claim-clm/clm-api + clm-web. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 20 | CLM-027: Implement supporting UX or orchestration for Redlining and Track Changes | `enhancement` | Implement the supporting integration, UI, or orchestration work needed to complete Redlining and Track Changes, especially across adjacent services and shared platform capabilities. Capture failure handling, idempotency, and backward compatibility so the feature behaves predictably under real contract workloads. |
| 21 | CLM-027: Add tests and docs for Redlining and Track Changes | `enhancement`, `testing`, `documentation` | Create unit, integration, and regression coverage for Redlining and Track Changes with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 22 | CLM-025: Design domain and API contract for Approval Delegation | `enhancement` | Define the Phase 2 reference architecture for Approval Delegation in claim-clm/clm-workflow-engine, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 23 | CLM-025: Implement core service logic for Approval Delegation | `enhancement` | Build the domain logic, APIs, worker orchestration, and persistence changes required for Approval Delegation in claim-clm/clm-workflow-engine. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 24 | CLM-025: Implement supporting UX or orchestration for Approval Delegation | `enhancement` | Implement the supporting integration, UI, or orchestration work needed to complete Approval Delegation, especially across adjacent services and shared platform capabilities. Capture failure handling, idempotency, and backward compatibility so the feature behaves predictably under real contract workloads. |
| 25 | CLM-025: Add tests and docs for Approval Delegation | `enhancement`, `testing`, `documentation` | Create unit, integration, and regression coverage for Approval Delegation with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 26 | CLM-036: Design Webhook Framework domain model | `enhancement`, `integration` | Define the Phase 2 reference architecture for Webhook Framework in claim-clm/clm-webhook-dispatcher, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 27 | CLM-036: Implement Webhook Framework feature flow | `enhancement`, `integration` | Build the domain logic, APIs, worker orchestration, and persistence changes required for Webhook Framework in claim-clm/clm-webhook-dispatcher. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 28 | CLM-036: Add tests and docs for Webhook Framework | `enhancement`, `integration`, `testing`, `documentation` | Create unit, integration, and regression coverage for Webhook Framework with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 29 | CLM-040: Design domain and API contract for SCIM User Provisioning | `enhancement` | Define the Phase 2 reference architecture for SCIM User Provisioning in claim-clm/clm-api, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 30 | CLM-040: Implement core service logic for SCIM User Provisioning | `enhancement` | Build the domain logic, APIs, worker orchestration, and persistence changes required for SCIM User Provisioning in claim-clm/clm-api. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 31 | CLM-040: Implement supporting UX or orchestration for SCIM User Provisioning | `enhancement` | Implement the supporting integration, UI, or orchestration work needed to complete SCIM User Provisioning, especially across adjacent services and shared platform capabilities. Capture failure handling, idempotency, and backward compatibility so the feature behaves predictably under real contract workloads. |
| 32 | CLM-040: Add tests and docs for SCIM User Provisioning | `enhancement`, `testing`, `documentation` | Create unit, integration, and regression coverage for SCIM User Provisioning with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 33 | CLM-038: Design domain and API contract for Audit Log Search | `enhancement`, `integration` | Define the Phase 2 reference architecture for Audit Log Search in claim-clm/clm-api, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 34 | CLM-038: Implement core service logic for Audit Log Search | `enhancement`, `integration` | Build the domain logic, APIs, worker orchestration, and persistence changes required for Audit Log Search in claim-clm/clm-api. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 35 | CLM-038: Implement supporting UX or orchestration for Audit Log Search | `enhancement`, `integration` | Implement the supporting integration, UI, or orchestration work needed to complete Audit Log Search, especially across adjacent services and shared platform capabilities. Capture failure handling, idempotency, and backward compatibility so the feature behaves predictably under real contract workloads. |
| 36 | CLM-038: Add tests and docs for Audit Log Search | `enhancement`, `integration`, `testing`, `documentation` | Create unit, integration, and regression coverage for Audit Log Search with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 37 | CLM-037: Design domain and API contract for Compliance Reporting | `enhancement` | Define the Phase 2 reference architecture for Compliance Reporting in claim-clm/clm-api + clm-web, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 38 | CLM-037: Implement core service logic for Compliance Reporting | `enhancement` | Build the domain logic, APIs, worker orchestration, and persistence changes required for Compliance Reporting in claim-clm/clm-api + clm-web. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 39 | CLM-037: Implement supporting UX or orchestration for Compliance Reporting | `enhancement` | Implement the supporting integration, UI, or orchestration work needed to complete Compliance Reporting, especially across adjacent services and shared platform capabilities. Capture failure handling, idempotency, and backward compatibility so the feature behaves predictably under real contract workloads. |
| 40 | CLM-037: Add tests and docs for Compliance Reporting | `enhancement`, `testing`, `documentation` | Create unit, integration, and regression coverage for Compliance Reporting with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |

### claim-clm-ai
| # | Issue Title | Labels | Description |
|---:|---|---|---|
| 1 | CLM-030: Define model plan for Key Term Extraction | `ai/ml` | Define the Phase 2 reference architecture for Key Term Extraction in claim-clm-ai/clm-ai-extractor, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 2 | CLM-030: Implement inference pipeline for Key Term Extraction | `ai/ml` | Implement the model invocation path, feature pipeline, and service endpoints required for Key Term Extraction in claim-clm-ai/clm-ai-extractor. Make the flow deterministic where possible, version prompts or models explicitly, and keep releases safe with feature flags or canary controls. |
| 3 | CLM-030: Add evaluation coverage for Key Term Extraction | `ai/ml`, `testing` | Create evaluation and regression coverage for Key Term Extraction with golden datasets, prompt or model version checks, and threshold-based quality gates. Include monitoring expectations and manual-review scenarios so the AI feature can be released with measurable confidence. |
| 4 | CLM-030: Document usage and operations for Key Term Extraction | `ai/ml`, `documentation` | Document how Key Term Extraction is configured, evaluated, and supported in claim-clm-ai, including API contracts, model-version controls, and reviewer guidance. The final documentation should explain operational limits, feedback loops, and release steps for both engineering and legal stakeholders. |
| 5 | CLM-029: Define model plan for Contract Summarization | `ai/ml` | Define the Phase 2 reference architecture for Contract Summarization in claim-clm-ai/clm-ai-summarizer, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 6 | CLM-029: Implement inference pipeline for Contract Summarization | `ai/ml` | Implement the model invocation path, feature pipeline, and service endpoints required for Contract Summarization in claim-clm-ai/clm-ai-summarizer. Make the flow deterministic where possible, version prompts or models explicitly, and keep releases safe with feature flags or canary controls. |
| 7 | CLM-029: Add evaluation coverage for Contract Summarization | `ai/ml`, `testing` | Create evaluation and regression coverage for Contract Summarization with golden datasets, prompt or model version checks, and threshold-based quality gates. Include monitoring expectations and manual-review scenarios so the AI feature can be released with measurable confidence. |
| 8 | CLM-029: Document usage and operations for Contract Summarization | `ai/ml`, `documentation` | Document how Contract Summarization is configured, evaluated, and supported in claim-clm-ai, including API contracts, model-version controls, and reviewer guidance. The final documentation should explain operational limits, feedback loops, and release steps for both engineering and legal stakeholders. |
| 9 | CLM-031: Define model plan for Semantic Contract Search | `ai/ml`, `integration` | Define the Phase 2 reference architecture for Semantic Contract Search in claim-clm-ai/clm-ai-search, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 10 | CLM-031: Implement inference pipeline for Semantic Contract Search | `ai/ml`, `integration` | Implement the model invocation path, feature pipeline, and service endpoints required for Semantic Contract Search in claim-clm-ai/clm-ai-search. Make the flow deterministic where possible, version prompts or models explicitly, and keep releases safe with feature flags or canary controls. |
| 11 | CLM-031: Add evaluation coverage for Semantic Contract Search | `ai/ml`, `integration`, `testing` | Create evaluation and regression coverage for Semantic Contract Search with golden datasets, prompt or model version checks, and threshold-based quality gates. Include monitoring expectations and manual-review scenarios so the AI feature can be released with measurable confidence. |
| 12 | CLM-031: Document usage and operations for Semantic Contract Search | `ai/ml`, `integration`, `documentation` | Document how Semantic Contract Search is configured, evaluated, and supported in claim-clm-ai, including API contracts, model-version controls, and reviewer guidance. The final documentation should explain operational limits, feedback loops, and release steps for both engineering and legal stakeholders. |
| 13 | CLM-032: Define model plan for Clause Deviation Detection | `ai/ml` | Define the Phase 2 reference architecture for Clause Deviation Detection in claim-clm-ai/clm-ai-deviation, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 14 | CLM-032: Implement inference pipeline for Clause Deviation Detection | `ai/ml` | Implement the model invocation path, feature pipeline, and service endpoints required for Clause Deviation Detection in claim-clm-ai/clm-ai-deviation. Make the flow deterministic where possible, version prompts or models explicitly, and keep releases safe with feature flags or canary controls. |
| 15 | CLM-032: Add evaluation coverage for Clause Deviation Detection | `ai/ml`, `testing` | Create evaluation and regression coverage for Clause Deviation Detection with golden datasets, prompt or model version checks, and threshold-based quality gates. Include monitoring expectations and manual-review scenarios so the AI feature can be released with measurable confidence. |
| 16 | CLM-032: Document usage and operations for Clause Deviation Detection | `ai/ml`, `documentation` | Document how Clause Deviation Detection is configured, evaluated, and supported in claim-clm-ai, including API contracts, model-version controls, and reviewer guidance. The final documentation should explain operational limits, feedback loops, and release steps for both engineering and legal stakeholders. |

### claim-clm-integration
| # | Issue Title | Labels | Description |
|---:|---|---|---|
| 1 | CLM-020: Design integration contract for E-Signature Integration | `integration` | Define the Phase 2 reference architecture for E-Signature Integration in claim-clm-integration/clm-esign-adapter, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 2 | CLM-020: Implement connector workflow for E-Signature Integration | `integration` | Build the connector endpoints, authentication handling, payload mapping, and sync jobs required for E-Signature Integration in claim-clm-integration/clm-esign-adapter. The implementation should handle idempotency, external API limits, and retry-safe state transitions so partner systems stay in sync. |
| 3 | CLM-020: Add observability and failure handling for E-Signature Integration | `integration`, `infrastructure` | Add the delivery, policy, and operational plumbing for E-Signature Integration so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 2 readiness. |
| 4 | CLM-020: Document configuration for E-Signature Integration | `integration`, `documentation` | Document how E-Signature Integration is configured, consumed, and supported in claim-clm-integration, including API contracts, operational runbooks, and rollback guidance as applicable. The final documentation should be detailed enough for developers, QA, and platform support to onboard without tribal knowledge. |

### claim-clm-portal
| # | Issue Title | Labels | Description |
|---:|---|---|---|
| 1 | CLM-028: Design external user architecture for Counterparty Portal | `enhancement` | Define the Phase 2 reference architecture for Counterparty Portal in claim-clm-portal/clm-counterparty-portal, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 2 | CLM-028: Implement backend flows for Counterparty Portal | `enhancement` | Implement the backend services and portal flows required for Counterparty Portal in claim-clm-portal/clm-counterparty-portal. Cover invitation handling, session lifecycle, authorization boundaries, and auditability so external-user scenarios remain secure and supportable. |
| 3 | CLM-028: Implement portal experience for Counterparty Portal | `enhancement` | Build the end-user portal experience for Counterparty Portal, including navigation, task states, notifications, and cross-journey validation. The workflow should feel coherent for guest or counterparty users while preserving internal review and approval boundaries. |
| 4 | CLM-028: Add security and end-to-end tests for Counterparty Portal | `enhancement`, `testing` | Create unit, integration, and regression coverage for Counterparty Portal with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 5 | CLM-028: Document onboarding and support for Counterparty Portal | `enhancement`, `documentation` | Document how Counterparty Portal is configured, consumed, and supported in claim-clm-portal, including API contracts, operational runbooks, and rollback guidance as applicable. The final documentation should be detailed enough for developers, QA, and platform support to onboard without tribal knowledge. |
| 6 | CLM-026: Design external user architecture for External Party Workflow | `enhancement` | Define the Phase 2 reference architecture for External Party Workflow in claim-clm-portal/clm-counterparty-portal + clm-guest-auth-service, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 7 | CLM-026: Implement backend flows for External Party Workflow | `enhancement` | Implement the backend services and portal flows required for External Party Workflow in claim-clm-portal/clm-counterparty-portal + clm-guest-auth-service. Cover invitation handling, session lifecycle, authorization boundaries, and auditability so external-user scenarios remain secure and supportable. |
| 8 | CLM-026: Implement portal experience for External Party Workflow | `enhancement` | Build the end-user portal experience for External Party Workflow, including navigation, task states, notifications, and cross-journey validation. The workflow should feel coherent for guest or counterparty users while preserving internal review and approval boundaries. |
| 9 | CLM-026: Add security and end-to-end tests for External Party Workflow | `enhancement`, `testing` | Create unit, integration, and regression coverage for External Party Workflow with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 10 | CLM-026: Document onboarding and support for External Party Workflow | `enhancement`, `documentation` | Document how External Party Workflow is configured, consumed, and supported in claim-clm-portal, including API contracts, operational runbooks, and rollback guidance as applicable. The final documentation should be detailed enough for developers, QA, and platform support to onboard without tribal knowledge. |

## Phase 3 — Advanced Features

### claim-clm-infra
| # | Issue Title | Labels | Description |
|---:|---|---|---|
| 1 | CLM-052: Design Data Loss Prevention architecture | `infrastructure` | Define the Phase 3 reference architecture for Data Loss Prevention in claim-clm-infra/Purview DLP, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 2 | CLM-052: Implement Terraform or Bicep for Data Loss Prevention | `infrastructure` | Author reusable Terraform or Bicep modules, Helm values, and Azure policy assets for Data Loss Prevention within Purview DLP. Ensure naming, private networking, secret references, RBAC bindings, and outputs are standardized so downstream environments can consume the platform layer consistently. |
| 3 | CLM-052: Configure policy and pipeline support for Data Loss Prevention | `infrastructure` | Add the delivery, policy, and operational plumbing for Data Loss Prevention so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 3 readiness. |
| 4 | CLM-052: Document operations for Data Loss Prevention | `infrastructure`, `documentation` | Document how Data Loss Prevention is provisioned, validated, and operated in claim-clm-infra, including environment prerequisites, emergency procedures, and rollback steps. The final runbook should be detailed enough for platform engineers and on-call responders to support the feature without tribal knowledge. |
| 5 | CLM-057: Design Multi-Tenancy reference architecture | `infrastructure` | Define the Phase 3 reference architecture for Multi-Tenancy in claim-clm-infra/cross-cutting, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 6 | CLM-057: Provision Terraform and Helm baseline for Multi-Tenancy | `infrastructure` | Author reusable Terraform or Bicep modules, Helm values, and Azure policy assets for Multi-Tenancy within cross-cutting. Ensure naming, private networking, secret references, RBAC bindings, and outputs are standardized so downstream environments can consume the platform layer consistently. |
| 7 | CLM-057: Configure policy, GitOps, and release controls for Multi-Tenancy | `infrastructure` | Add the delivery, policy, and operational plumbing for Multi-Tenancy so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 3 readiness. |
| 8 | CLM-057: Validate resilience and security for Multi-Tenancy | `infrastructure`, `testing` | Add validation coverage for Multi-Tenancy using policy checks, environment smoke tests, and failure-path exercises. The final test suite should prove the infrastructure can be promoted safely and that operations teams can detect drift or broken dependencies quickly. |
| 9 | CLM-057: Publish runbooks for Multi-Tenancy | `infrastructure`, `documentation` | Document how Multi-Tenancy is provisioned, validated, and operated in claim-clm-infra, including environment prerequisites, emergency procedures, and rollback steps. The final runbook should be detailed enough for platform engineers and on-call responders to support the feature without tribal knowledge. |
| 10 | CLM-058: Design Disaster Recovery reference architecture | `infrastructure` | Define the Phase 3 reference architecture for Disaster Recovery in claim-clm-infra/modules/dr, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 11 | CLM-058: Provision Terraform and Helm baseline for Disaster Recovery | `infrastructure` | Author reusable Terraform or Bicep modules, Helm values, and Azure policy assets for Disaster Recovery within modules/dr. Ensure naming, private networking, secret references, RBAC bindings, and outputs are standardized so downstream environments can consume the platform layer consistently. |
| 12 | CLM-058: Configure policy, GitOps, and release controls for Disaster Recovery | `infrastructure` | Add the delivery, policy, and operational plumbing for Disaster Recovery so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 3 readiness. |
| 13 | CLM-058: Validate resilience and security for Disaster Recovery | `infrastructure`, `testing` | Add validation coverage for Disaster Recovery using policy checks, environment smoke tests, and failure-path exercises. The final test suite should prove the infrastructure can be promoted safely and that operations teams can detect drift or broken dependencies quickly. |
| 14 | CLM-058: Publish runbooks for Disaster Recovery | `infrastructure`, `documentation` | Document how Disaster Recovery is provisioned, validated, and operated in claim-clm-infra, including environment prerequisites, emergency procedures, and rollback steps. The final runbook should be detailed enough for platform engineers and on-call responders to support the feature without tribal knowledge. |

### claim-clm-storage
| # | Issue Title | Labels | Description |
|---:|---|---|---|
| 1 | CLM-051: Design Document Classification data flow and contracts | `enhancement` | Define the Phase 3 reference architecture for Document Classification in claim-clm-storage/clm-doc-classifier, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 2 | CLM-051: Implement service components for Document Classification | `enhancement` | Implement storage APIs, persistence changes, and background processing for Document Classification in claim-clm-storage/clm-doc-classifier. Cover lifecycle rules, metadata consistency, retry behavior, and failure recovery between blob storage and any dependent indexing or retrieval components. |
| 3 | CLM-051: Integrate storage and indexing dependencies for Document Classification | `enhancement`, `infrastructure` | Add the delivery, policy, and operational plumbing for Document Classification so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 3 readiness. |
| 4 | CLM-051: Add tests and operations guide for Document Classification | `enhancement`, `testing`, `documentation` | Create unit, integration, and regression coverage for Document Classification with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |

### claim-clm
| # | Issue Title | Labels | Description |
|---:|---|---|---|
| 1 | CLM-048: Design domain and API contract for Saved Searches and Alerts | `enhancement`, `integration` | Define the Phase 3 reference architecture for Saved Searches and Alerts in claim-clm/clm-search-service, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 2 | CLM-048: Implement core service logic for Saved Searches and Alerts | `enhancement`, `integration` | Build the domain logic, APIs, worker orchestration, and persistence changes required for Saved Searches and Alerts in claim-clm/clm-search-service. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 3 | CLM-048: Implement supporting UX or orchestration for Saved Searches and Alerts | `enhancement`, `integration` | Implement the supporting integration, UI, or orchestration work needed to complete Saved Searches and Alerts, especially across adjacent services and shared platform capabilities. Capture failure handling, idempotency, and backward compatibility so the feature behaves predictably under real contract workloads. |
| 4 | CLM-048: Add tests and docs for Saved Searches and Alerts | `enhancement`, `integration`, `testing`, `documentation` | Create unit, integration, and regression coverage for Saved Searches and Alerts with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 5 | CLM-049: Design end-to-end architecture for Executive Dashboard | `enhancement` | Define the Phase 3 reference architecture for Executive Dashboard in claim-clm/clm-web + clm-api, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 6 | CLM-049: Implement backend services for Executive Dashboard | `enhancement` | Build the domain logic, APIs, worker orchestration, and persistence changes required for Executive Dashboard in claim-clm/clm-web + clm-api. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 7 | CLM-049: Implement UI, workflow, or integration support for Executive Dashboard | `enhancement` | Implement the supporting integration, UI, or orchestration work needed to complete Executive Dashboard, especially across adjacent services and shared platform capabilities. Capture failure handling, idempotency, and backward compatibility so the feature behaves predictably under real contract workloads. |
| 8 | CLM-049: Add tests and telemetry for Executive Dashboard | `enhancement`, `testing` | Create unit, integration, and regression coverage for Executive Dashboard with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 9 | CLM-049: Document APIs and rollout for Executive Dashboard | `enhancement`, `documentation` | Document how Executive Dashboard is configured, consumed, and supported in claim-clm, including API contracts, operational runbooks, and rollback guidance as applicable. The final documentation should be detailed enough for developers, QA, and platform support to onboard without tribal knowledge. |
| 10 | CLM-050: Design end-to-end architecture for Custom Report Builder | `enhancement` | Define the Phase 3 reference architecture for Custom Report Builder in claim-clm/clm-web + clm-api, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 11 | CLM-050: Implement backend services for Custom Report Builder | `enhancement` | Build the domain logic, APIs, worker orchestration, and persistence changes required for Custom Report Builder in claim-clm/clm-web + clm-api. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 12 | CLM-050: Implement UI, workflow, or integration support for Custom Report Builder | `enhancement` | Implement the supporting integration, UI, or orchestration work needed to complete Custom Report Builder, especially across adjacent services and shared platform capabilities. Capture failure handling, idempotency, and backward compatibility so the feature behaves predictably under real contract workloads. |
| 13 | CLM-050: Add tests and telemetry for Custom Report Builder | `enhancement`, `testing` | Create unit, integration, and regression coverage for Custom Report Builder with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 14 | CLM-050: Document APIs and rollout for Custom Report Builder | `enhancement`, `documentation` | Document how Custom Report Builder is configured, consumed, and supported in claim-clm, including API contracts, operational runbooks, and rollback guidance as applicable. The final documentation should be detailed enough for developers, QA, and platform support to onboard without tribal knowledge. |
| 15 | CLM-055: Design domain and API contract for Milestone and Renewal Tracking | `enhancement` | Define the Phase 3 reference architecture for Milestone and Renewal Tracking in claim-clm/clm-api + clm-sla-enforcer, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 16 | CLM-055: Implement core service logic for Milestone and Renewal Tracking | `enhancement` | Build the domain logic, APIs, worker orchestration, and persistence changes required for Milestone and Renewal Tracking in claim-clm/clm-api + clm-sla-enforcer. Make the change migration-safe and environment-aware so it can be promoted through dev, test, and production without manual fixes. |
| 17 | CLM-055: Implement supporting UX or orchestration for Milestone and Renewal Tracking | `enhancement` | Implement the supporting integration, UI, or orchestration work needed to complete Milestone and Renewal Tracking, especially across adjacent services and shared platform capabilities. Capture failure handling, idempotency, and backward compatibility so the feature behaves predictably under real contract workloads. |
| 18 | CLM-055: Add tests and docs for Milestone and Renewal Tracking | `enhancement`, `testing`, `documentation` | Create unit, integration, and regression coverage for Milestone and Renewal Tracking with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |

### claim-clm-ai
| # | Issue Title | Labels | Description |
|---:|---|---|---|
| 1 | CLM-041: Define model and evaluation plan for Contract Risk Scoring | `ai/ml` | Define the Phase 3 reference architecture for Contract Risk Scoring in claim-clm-ai/clm-ai-risk-scorer, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 2 | CLM-041: Implement inference pipeline for Contract Risk Scoring | `ai/ml` | Implement the model invocation path, feature pipeline, and service endpoints required for Contract Risk Scoring in claim-clm-ai/clm-ai-risk-scorer. Make the flow deterministic where possible, version prompts or models explicitly, and keep releases safe with feature flags or canary controls. |
| 3 | CLM-041: Build guardrails and review flow for Contract Risk Scoring | `ai/ml` | Add the supporting evaluation workflow, reviewer handoff, and guardrail logic needed to make Contract Risk Scoring production-ready. Include confidence thresholds, fallback behavior, and traceability so legal teams can understand when model output should be accepted, reviewed, or rejected. |
| 4 | CLM-041: Add evaluation and monitoring for Contract Risk Scoring | `ai/ml`, `testing` | Create evaluation and regression coverage for Contract Risk Scoring with golden datasets, prompt or model version checks, and threshold-based quality gates. Include monitoring expectations and manual-review scenarios so the AI feature can be released with measurable confidence. |
| 5 | CLM-041: Document Contract Risk Scoring API and model operations | `ai/ml`, `documentation` | Document how Contract Risk Scoring is configured, evaluated, and supported in claim-clm-ai, including API contracts, model-version controls, and reviewer guidance. The final documentation should explain operational limits, feedback loops, and release steps for both engineering and legal stakeholders. |
| 6 | CLM-042: Define model plan for PII Detection and Redaction | `ai/ml`, `security` | Define the Phase 3 reference architecture for PII Detection and Redaction in claim-clm-ai/clm-ai-pii, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 7 | CLM-042: Implement inference pipeline for PII Detection and Redaction | `ai/ml`, `security` | Implement the model invocation path, feature pipeline, and service endpoints required for PII Detection and Redaction in claim-clm-ai/clm-ai-pii. Make the flow deterministic where possible, version prompts or models explicitly, and keep releases safe with feature flags or canary controls. |
| 8 | CLM-042: Add evaluation coverage for PII Detection and Redaction | `ai/ml`, `security`, `testing` | Create evaluation and regression coverage for PII Detection and Redaction with golden datasets, prompt or model version checks, and threshold-based quality gates. Include monitoring expectations and manual-review scenarios so the AI feature can be released with measurable confidence. |
| 9 | CLM-042: Document usage and operations for PII Detection and Redaction | `ai/ml`, `security`, `documentation` | Document how PII Detection and Redaction is configured, evaluated, and supported in claim-clm-ai, including API contracts, model-version controls, and reviewer guidance. The final documentation should explain operational limits, feedback loops, and release steps for both engineering and legal stakeholders. |
| 10 | CLM-054: Define model plan for Obligation Extraction | `ai/ml` | Define the Phase 3 reference architecture for Obligation Extraction in claim-clm-ai/clm-ai-extractor, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 11 | CLM-054: Implement inference pipeline for Obligation Extraction | `ai/ml` | Implement the model invocation path, feature pipeline, and service endpoints required for Obligation Extraction in claim-clm-ai/clm-ai-extractor. Make the flow deterministic where possible, version prompts or models explicitly, and keep releases safe with feature flags or canary controls. |
| 12 | CLM-054: Add evaluation coverage for Obligation Extraction | `ai/ml`, `testing` | Create evaluation and regression coverage for Obligation Extraction with golden datasets, prompt or model version checks, and threshold-based quality gates. Include monitoring expectations and manual-review scenarios so the AI feature can be released with measurable confidence. |
| 13 | CLM-054: Document usage and operations for Obligation Extraction | `ai/ml`, `documentation` | Document how Obligation Extraction is configured, evaluated, and supported in claim-clm-ai, including API contracts, model-version controls, and reviewer guidance. The final documentation should explain operational limits, feedback loops, and release steps for both engineering and legal stakeholders. |
| 14 | CLM-044: Define model and evaluation plan for AI Contract Drafting Assistant | `ai/ml` | Define the Phase 3 reference architecture for AI Contract Drafting Assistant in claim-clm-ai/clm-ai-drafter, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 15 | CLM-044: Implement inference pipeline for AI Contract Drafting Assistant | `ai/ml` | Implement the model invocation path, feature pipeline, and service endpoints required for AI Contract Drafting Assistant in claim-clm-ai/clm-ai-drafter. Make the flow deterministic where possible, version prompts or models explicitly, and keep releases safe with feature flags or canary controls. |
| 16 | CLM-044: Build guardrails and review flow for AI Contract Drafting Assistant | `ai/ml` | Add the supporting evaluation workflow, reviewer handoff, and guardrail logic needed to make AI Contract Drafting Assistant production-ready. Include confidence thresholds, fallback behavior, and traceability so legal teams can understand when model output should be accepted, reviewed, or rejected. |
| 17 | CLM-044: Add evaluation and monitoring for AI Contract Drafting Assistant | `ai/ml`, `testing` | Create evaluation and regression coverage for AI Contract Drafting Assistant with golden datasets, prompt or model version checks, and threshold-based quality gates. Include monitoring expectations and manual-review scenarios so the AI feature can be released with measurable confidence. |
| 18 | CLM-044: Document AI Contract Drafting Assistant API and model operations | `ai/ml`, `documentation` | Document how AI Contract Drafting Assistant is configured, evaluated, and supported in claim-clm-ai, including API contracts, model-version controls, and reviewer guidance. The final documentation should explain operational limits, feedback loops, and release steps for both engineering and legal stakeholders. |
| 19 | CLM-043: Define model plan for Contract Renewal Prediction | `ai/ml` | Define the Phase 3 reference architecture for Contract Renewal Prediction in claim-clm-ai/clm-ai-prediction, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 20 | CLM-043: Implement inference pipeline for Contract Renewal Prediction | `ai/ml` | Implement the model invocation path, feature pipeline, and service endpoints required for Contract Renewal Prediction in claim-clm-ai/clm-ai-prediction. Make the flow deterministic where possible, version prompts or models explicitly, and keep releases safe with feature flags or canary controls. |
| 21 | CLM-043: Add evaluation coverage for Contract Renewal Prediction | `ai/ml`, `testing` | Create evaluation and regression coverage for Contract Renewal Prediction with golden datasets, prompt or model version checks, and threshold-based quality gates. Include monitoring expectations and manual-review scenarios so the AI feature can be released with measurable confidence. |
| 22 | CLM-043: Document usage and operations for Contract Renewal Prediction | `ai/ml`, `documentation` | Document how Contract Renewal Prediction is configured, evaluated, and supported in claim-clm-ai, including API contracts, model-version controls, and reviewer guidance. The final documentation should explain operational limits, feedback loops, and release steps for both engineering and legal stakeholders. |

### claim-clm-integration
| # | Issue Title | Labels | Description |
|---:|---|---|---|
| 1 | CLM-045: Design integration contract for Salesforce Connector | `integration` | Define the Phase 3 reference architecture for Salesforce Connector in claim-clm-integration/clm-salesforce-connector, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 2 | CLM-045: Implement connector workflow for Salesforce Connector | `integration` | Build the connector endpoints, authentication handling, payload mapping, and sync jobs required for Salesforce Connector in claim-clm-integration/clm-salesforce-connector. The implementation should handle idempotency, external API limits, and retry-safe state transitions so partner systems stay in sync. |
| 3 | CLM-045: Add observability and failure handling for Salesforce Connector | `integration`, `infrastructure` | Add the delivery, policy, and operational plumbing for Salesforce Connector so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 3 readiness. |
| 4 | CLM-045: Document configuration for Salesforce Connector | `integration`, `documentation` | Document how Salesforce Connector is configured, consumed, and supported in claim-clm-integration, including API contracts, operational runbooks, and rollback guidance as applicable. The final documentation should be detailed enough for developers, QA, and platform support to onboard without tribal knowledge. |
| 5 | CLM-046: Design integration contract for SAP Integration | `integration` | Define the Phase 3 reference architecture for SAP Integration in claim-clm-integration/clm-sap-connector, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 6 | CLM-046: Implement connector workflow for SAP Integration | `integration` | Build the connector endpoints, authentication handling, payload mapping, and sync jobs required for SAP Integration in claim-clm-integration/clm-sap-connector. The implementation should handle idempotency, external API limits, and retry-safe state transitions so partner systems stay in sync. |
| 7 | CLM-046: Add observability and failure handling for SAP Integration | `integration`, `infrastructure` | Add the delivery, policy, and operational plumbing for SAP Integration so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 3 readiness. |
| 8 | CLM-046: Document configuration for SAP Integration | `integration`, `documentation` | Document how SAP Integration is configured, consumed, and supported in claim-clm-integration, including API contracts, operational runbooks, and rollback guidance as applicable. The final documentation should be detailed enough for developers, QA, and platform support to onboard without tribal knowledge. |
| 9 | CLM-047: Design integration contract for Microsoft Teams Integration | `integration` | Define the Phase 3 reference architecture for Microsoft Teams Integration in claim-clm-integration/clm-teams-bot, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 10 | CLM-047: Implement connector workflow for Microsoft Teams Integration | `integration` | Build the connector endpoints, authentication handling, payload mapping, and sync jobs required for Microsoft Teams Integration in claim-clm-integration/clm-teams-bot. The implementation should handle idempotency, external API limits, and retry-safe state transitions so partner systems stay in sync. |
| 11 | CLM-047: Add observability and failure handling for Microsoft Teams Integration | `integration`, `infrastructure` | Add the delivery, policy, and operational plumbing for Microsoft Teams Integration so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 3 readiness. |
| 12 | CLM-047: Document configuration for Microsoft Teams Integration | `integration`, `documentation` | Document how Microsoft Teams Integration is configured, consumed, and supported in claim-clm-integration, including API contracts, operational runbooks, and rollback guidance as applicable. The final documentation should be detailed enough for developers, QA, and platform support to onboard without tribal knowledge. |

### claim-clm-portal
| # | Issue Title | Labels | Description |
|---:|---|---|---|
| 1 | CLM-056: Design portal workflow for Guest User Access | `enhancement`, `security` | Define the Phase 3 reference architecture for Guest User Access in claim-clm-portal/clm-guest-auth-service, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 2 | CLM-056: Implement service changes for Guest User Access | `enhancement`, `security` | Implement the backend services and portal flows required for Guest User Access in claim-clm-portal/clm-guest-auth-service. Cover invitation handling, session lifecycle, authorization boundaries, and auditability so external-user scenarios remain secure and supportable. |
| 3 | CLM-056: Add end-to-end tests for Guest User Access | `enhancement`, `security`, `testing` | Create unit, integration, and regression coverage for Guest User Access with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 4 | CLM-056: Document operations for Guest User Access | `enhancement`, `security`, `documentation` | Document how Guest User Access is configured, consumed, and supported in claim-clm-portal, including API contracts, operational runbooks, and rollback guidance as applicable. The final documentation should be detailed enough for developers, QA, and platform support to onboard without tribal knowledge. |

## Phase 4 — Innovation & Optimization

### claim-clm-infra
| # | Issue Title | Labels | Description |
|---:|---|---|---|
| 1 | CLM-069: Design Chaos Engineering architecture | `infrastructure` | Define the Phase 4 reference architecture for Chaos Engineering in claim-clm-infra/Chaos Studio, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 2 | CLM-069: Implement Terraform or Bicep for Chaos Engineering | `infrastructure` | Author reusable Terraform or Bicep modules, Helm values, and Azure policy assets for Chaos Engineering within Chaos Studio. Ensure naming, private networking, secret references, RBAC bindings, and outputs are standardized so downstream environments can consume the platform layer consistently. |
| 3 | CLM-069: Configure policy and pipeline support for Chaos Engineering | `infrastructure` | Add the delivery, policy, and operational plumbing for Chaos Engineering so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 4 readiness. |
| 4 | CLM-069: Document operations for Chaos Engineering | `infrastructure`, `documentation` | Document how Chaos Engineering is provisioned, validated, and operated in claim-clm-infra, including environment prerequisites, emergency procedures, and rollback steps. The final runbook should be detailed enough for platform engineers and on-call responders to support the feature without tribal knowledge. |
| 5 | CLM-070: Design Zero Trust Network Access architecture | `infrastructure`, `security` | Define the Phase 4 reference architecture for Zero Trust Network Access in claim-clm-infra/Entra GSA, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 6 | CLM-070: Implement Terraform or Bicep for Zero Trust Network Access | `infrastructure`, `security` | Author reusable Terraform or Bicep modules, Helm values, and Azure policy assets for Zero Trust Network Access within Entra GSA. Ensure naming, private networking, secret references, RBAC bindings, and outputs are standardized so downstream environments can consume the platform layer consistently. |
| 7 | CLM-070: Configure policy and pipeline support for Zero Trust Network Access | `infrastructure`, `security` | Add the delivery, policy, and operational plumbing for Zero Trust Network Access so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 4 readiness. |
| 8 | CLM-070: Document operations for Zero Trust Network Access | `infrastructure`, `security`, `documentation` | Document how Zero Trust Network Access is provisioned, validated, and operated in claim-clm-infra, including environment prerequisites, emergency procedures, and rollback steps. The final runbook should be detailed enough for platform engineers and on-call responders to support the feature without tribal knowledge. |
| 9 | CLM-071: Design FinOps and Cost Optimisation architecture | `infrastructure` | Define the Phase 4 reference architecture for FinOps and Cost Optimisation in claim-clm-infra/Kubecost + Azure Cost Mgmt, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 10 | CLM-071: Implement Terraform or Bicep for FinOps and Cost Optimisation | `infrastructure` | Author reusable Terraform or Bicep modules, Helm values, and Azure policy assets for FinOps and Cost Optimisation within Kubecost + Azure Cost Mgmt. Ensure naming, private networking, secret references, RBAC bindings, and outputs are standardized so downstream environments can consume the platform layer consistently. |
| 11 | CLM-071: Configure policy and pipeline support for FinOps and Cost Optimisation | `infrastructure` | Add the delivery, policy, and operational plumbing for FinOps and Cost Optimisation so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 4 readiness. |
| 12 | CLM-071: Document operations for FinOps and Cost Optimisation | `infrastructure`, `documentation` | Document how FinOps and Cost Optimisation is provisioned, validated, and operated in claim-clm-infra, including environment prerequisites, emergency procedures, and rollback steps. The final runbook should be detailed enough for platform engineers and on-call responders to support the feature without tribal knowledge. |

### claim-clm-storage
| # | Issue Title | Labels | Description |
|---:|---|---|---|
| 1 | CLM-068: Design Legal Hold Management API and data model | `enhancement` | Define the Phase 4 reference architecture for Legal Hold Management in claim-clm-storage/clm-storage-api, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 2 | CLM-068: Implement Legal Hold Management service workflow | `enhancement` | Implement storage APIs, persistence changes, and background processing for Legal Hold Management in claim-clm-storage/clm-storage-api. Cover lifecycle rules, metadata consistency, retry behavior, and failure recovery between blob storage and any dependent indexing or retrieval components. |
| 3 | CLM-068: Add tests and docs for Legal Hold Management | `enhancement`, `testing`, `documentation` | Create unit, integration, and regression coverage for Legal Hold Management with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |

### claim-clm-ai
| # | Issue Title | Labels | Description |
|---:|---|---|---|
| 1 | CLM-060: Define model plan for Multi-Language Contract Support | `ai/ml` | Define the Phase 4 reference architecture for Multi-Language Contract Support in claim-clm-ai/clm-ai-translator, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 2 | CLM-060: Implement inference pipeline for Multi-Language Contract Support | `ai/ml` | Implement the model invocation path, feature pipeline, and service endpoints required for Multi-Language Contract Support in claim-clm-ai/clm-ai-translator. Make the flow deterministic where possible, version prompts or models explicitly, and keep releases safe with feature flags or canary controls. |
| 3 | CLM-060: Add evaluation coverage for Multi-Language Contract Support | `ai/ml`, `testing` | Create evaluation and regression coverage for Multi-Language Contract Support with golden datasets, prompt or model version checks, and threshold-based quality gates. Include monitoring expectations and manual-review scenarios so the AI feature can be released with measurable confidence. |
| 4 | CLM-060: Document usage and operations for Multi-Language Contract Support | `ai/ml`, `documentation` | Document how Multi-Language Contract Support is configured, evaluated, and supported in claim-clm-ai, including API contracts, model-version controls, and reviewer guidance. The final documentation should explain operational limits, feedback loops, and release steps for both engineering and legal stakeholders. |
| 5 | CLM-059: Define model plan for Contract Benchmarking | `ai/ml` | Define the Phase 4 reference architecture for Contract Benchmarking in claim-clm-ai/clm-ai-prediction, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 6 | CLM-059: Implement inference pipeline for Contract Benchmarking | `ai/ml` | Implement the model invocation path, feature pipeline, and service endpoints required for Contract Benchmarking in claim-clm-ai/clm-ai-prediction. Make the flow deterministic where possible, version prompts or models explicitly, and keep releases safe with feature flags or canary controls. |
| 7 | CLM-059: Add evaluation coverage for Contract Benchmarking | `ai/ml`, `testing` | Create evaluation and regression coverage for Contract Benchmarking with golden datasets, prompt or model version checks, and threshold-based quality gates. Include monitoring expectations and manual-review scenarios so the AI feature can be released with measurable confidence. |
| 8 | CLM-059: Document usage and operations for Contract Benchmarking | `ai/ml`, `documentation` | Document how Contract Benchmarking is configured, evaluated, and supported in claim-clm-ai, including API contracts, model-version controls, and reviewer guidance. The final documentation should explain operational limits, feedback loops, and release steps for both engineering and legal stakeholders. |
| 9 | CLM-061: Define model plan for Sentiment and Tone Analysis | `ai/ml` | Define the Phase 4 reference architecture for Sentiment and Tone Analysis in claim-clm-ai/clm-ai-sentiment, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 10 | CLM-061: Implement inference pipeline for Sentiment and Tone Analysis | `ai/ml` | Implement the model invocation path, feature pipeline, and service endpoints required for Sentiment and Tone Analysis in claim-clm-ai/clm-ai-sentiment. Make the flow deterministic where possible, version prompts or models explicitly, and keep releases safe with feature flags or canary controls. |
| 11 | CLM-061: Add evaluation coverage for Sentiment and Tone Analysis | `ai/ml`, `testing` | Create evaluation and regression coverage for Sentiment and Tone Analysis with golden datasets, prompt or model version checks, and threshold-based quality gates. Include monitoring expectations and manual-review scenarios so the AI feature can be released with measurable confidence. |
| 12 | CLM-061: Document usage and operations for Sentiment and Tone Analysis | `ai/ml`, `documentation` | Document how Sentiment and Tone Analysis is configured, evaluated, and supported in claim-clm-ai, including API contracts, model-version controls, and reviewer guidance. The final documentation should explain operational limits, feedback loops, and release steps for both engineering and legal stakeholders. |
| 13 | CLM-066: Define model plan for Predictive Analytics | `ai/ml` | Define the Phase 4 reference architecture for Predictive Analytics in claim-clm-ai/clm-ai-prediction, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 14 | CLM-066: Implement inference pipeline for Predictive Analytics | `ai/ml` | Implement the model invocation path, feature pipeline, and service endpoints required for Predictive Analytics in claim-clm-ai/clm-ai-prediction. Make the flow deterministic where possible, version prompts or models explicitly, and keep releases safe with feature flags or canary controls. |
| 15 | CLM-066: Add evaluation coverage for Predictive Analytics | `ai/ml`, `testing` | Create evaluation and regression coverage for Predictive Analytics with golden datasets, prompt or model version checks, and threshold-based quality gates. Include monitoring expectations and manual-review scenarios so the AI feature can be released with measurable confidence. |
| 16 | CLM-066: Document usage and operations for Predictive Analytics | `ai/ml`, `documentation` | Document how Predictive Analytics is configured, evaluated, and supported in claim-clm-ai, including API contracts, model-version controls, and reviewer guidance. The final documentation should explain operational limits, feedback loops, and release steps for both engineering and legal stakeholders. |
| 17 | CLM-067: Define model plan for Contract Intelligence Weekly Digest | `ai/ml` | Define the Phase 4 reference architecture for Contract Intelligence Weekly Digest in claim-clm-ai/clm-ai-digest, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 18 | CLM-067: Implement inference pipeline for Contract Intelligence Weekly Digest | `ai/ml` | Implement the model invocation path, feature pipeline, and service endpoints required for Contract Intelligence Weekly Digest in claim-clm-ai/clm-ai-digest. Make the flow deterministic where possible, version prompts or models explicitly, and keep releases safe with feature flags or canary controls. |
| 19 | CLM-067: Add evaluation coverage for Contract Intelligence Weekly Digest | `ai/ml`, `testing` | Create evaluation and regression coverage for Contract Intelligence Weekly Digest with golden datasets, prompt or model version checks, and threshold-based quality gates. Include monitoring expectations and manual-review scenarios so the AI feature can be released with measurable confidence. |
| 20 | CLM-067: Document usage and operations for Contract Intelligence Weekly Digest | `ai/ml`, `documentation` | Document how Contract Intelligence Weekly Digest is configured, evaluated, and supported in claim-clm-ai, including API contracts, model-version controls, and reviewer guidance. The final documentation should explain operational limits, feedback loops, and release steps for both engineering and legal stakeholders. |
| 21 | CLM-062: Define model and evaluation plan for Voice and Chat Interface | `ai/ml` | Define the Phase 4 reference architecture for Voice and Chat Interface in claim-clm-ai/clm-ai-chatbot, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 22 | CLM-062: Implement inference pipeline for Voice and Chat Interface | `ai/ml` | Implement the model invocation path, feature pipeline, and service endpoints required for Voice and Chat Interface in claim-clm-ai/clm-ai-chatbot. Make the flow deterministic where possible, version prompts or models explicitly, and keep releases safe with feature flags or canary controls. |
| 23 | CLM-062: Build guardrails and review flow for Voice and Chat Interface | `ai/ml` | Add the supporting evaluation workflow, reviewer handoff, and guardrail logic needed to make Voice and Chat Interface production-ready. Include confidence thresholds, fallback behavior, and traceability so legal teams can understand when model output should be accepted, reviewed, or rejected. |
| 24 | CLM-062: Add evaluation and monitoring for Voice and Chat Interface | `ai/ml`, `testing` | Create evaluation and regression coverage for Voice and Chat Interface with golden datasets, prompt or model version checks, and threshold-based quality gates. Include monitoring expectations and manual-review scenarios so the AI feature can be released with measurable confidence. |
| 25 | CLM-062: Document Voice and Chat Interface API and model operations | `ai/ml`, `documentation` | Document how Voice and Chat Interface is configured, evaluated, and supported in claim-clm-ai, including API contracts, model-version controls, and reviewer guidance. The final documentation should explain operational limits, feedback loops, and release steps for both engineering and legal stakeholders. |

### claim-clm-integration
| # | Issue Title | Labels | Description |
|---:|---|---|---|
| 1 | CLM-063: Design connector contract for GraphQL API | `integration` | Define the Phase 4 reference architecture for GraphQL API in claim-clm-integration/clm-graphql-api, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 2 | CLM-063: Implement adapter and auth flow for GraphQL API | `integration` | Build the connector endpoints, authentication handling, payload mapping, and sync jobs required for GraphQL API in claim-clm-integration/clm-graphql-api. The implementation should handle idempotency, external API limits, and retry-safe state transitions so partner systems stay in sync. |
| 3 | CLM-063: Build sync, webhook, and retry handling for GraphQL API | `integration` | Implement the supporting webhook processing, scheduled sync orchestration, and failure-handling paths needed to complete GraphQL API. Include reconciliation jobs and support tooling so operational teams can recover from partial deliveries or partner-side outages. |
| 4 | CLM-063: Add sandbox integration tests for GraphQL API | `integration`, `testing` | Create unit, integration, and regression coverage for GraphQL API with scenarios for happy path, authorization, resilience, and failure recovery. Include test data, monitoring expectations, and clear validation steps so the feature can be signed off before release. |
| 5 | CLM-063: Document deployment and support steps for GraphQL API | `integration`, `documentation` | Document how GraphQL API is configured, consumed, and supported in claim-clm-integration, including API contracts, operational runbooks, and rollback guidance as applicable. The final documentation should be detailed enough for developers, QA, and platform support to onboard without tribal knowledge. |
| 6 | CLM-064: Design integration contract for Microsoft 365 Add-In | `integration` | Define the Phase 4 reference architecture for Microsoft 365 Add-In in claim-clm-integration/clm-m365-addin, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 7 | CLM-064: Implement connector workflow for Microsoft 365 Add-In | `integration` | Build the connector endpoints, authentication handling, payload mapping, and sync jobs required for Microsoft 365 Add-In in claim-clm-integration/clm-m365-addin. The implementation should handle idempotency, external API limits, and retry-safe state transitions so partner systems stay in sync. |
| 8 | CLM-064: Add observability and failure handling for Microsoft 365 Add-In | `integration`, `infrastructure` | Add the delivery, policy, and operational plumbing for Microsoft 365 Add-In so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 4 readiness. |
| 9 | CLM-064: Document configuration for Microsoft 365 Add-In | `integration`, `documentation` | Document how Microsoft 365 Add-In is configured, consumed, and supported in claim-clm-integration, including API contracts, operational runbooks, and rollback guidance as applicable. The final documentation should be detailed enough for developers, QA, and platform support to onboard without tribal knowledge. |
| 10 | CLM-065: Design integration contract for Power Automate Connector | `integration` | Define the Phase 4 reference architecture for Power Automate Connector in claim-clm-integration/clm-power-automate, covering boundaries, data flows, security expectations, and dependencies. Produce acceptance criteria, sequence diagrams, and rollout assumptions so downstream implementation issues can execute with minimal rework. |
| 11 | CLM-065: Implement connector workflow for Power Automate Connector | `integration` | Build the connector endpoints, authentication handling, payload mapping, and sync jobs required for Power Automate Connector in claim-clm-integration/clm-power-automate. The implementation should handle idempotency, external API limits, and retry-safe state transitions so partner systems stay in sync. |
| 12 | CLM-065: Add observability and failure handling for Power Automate Connector | `integration`, `infrastructure` | Add the delivery, policy, and operational plumbing for Power Automate Connector so deployments are automated and support teams can verify the feature in each environment. This includes Helm or Terraform wiring where applicable, observability hooks, and release gates aligned to Phase 4 readiness. |
| 13 | CLM-065: Document configuration for Power Automate Connector | `integration`, `documentation` | Document how Power Automate Connector is configured, consumed, and supported in claim-clm-integration, including API contracts, operational runbooks, and rollback guidance as applicable. The final documentation should be detailed enough for developers, QA, and platform support to onboard without tribal knowledge. |
